import { StyleSheet } from 'react-native'

export const commonStyles = StyleSheet.create({
  scrollView: {
    width: '100%',
    paddingHorizontal: 24,
    paddingTop: 16,
  },
  scrollViewContent: {
    rowGap: 16,
  },
})
